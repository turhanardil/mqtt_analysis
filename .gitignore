# ============================
#  ESP-IDF / receiver_mqtt
# ============================

# Ignore all build output for the ESP32 project
/receiver_mqtt/build/
/receiver_mqtt/*.bin
/receiver_mqtt/*.elf
/receiver_mqtt/*.map
/receiver_mqtt/compile_commands.json

# SDK config backup (automatically generated by ESP-IDF)
/receiver_mqtt/sdkconfig.old

# Partition CSV might be autogenerated or not needed in Git
# (uncomment if you want to ignore it)
/receiver_mqtt/partitions.csv

# ota.py is a Python script—leave it in source control,
# but ignore any local changes to it if it generates additional files
/receiver_mqtt/*.log

# ============================
#  Python / analyzer_training
# ============================

# Python cache files
**/__pycache__/
**/*.pyc

# Ignore any virtual‐environment or conda env folders
venv/
.env/
env/

# Raw CSV dumps (replace/match your actual CSV names if needed)
# If you want to keep small training CSVs under source control, remove these lines.
/analyzer_training/*.csv

# TensorFlow-saved “variables” and “assets” directories (often large)
/analyzer_training/tfidf_random_forest_model/variables/
/analyzer_training/tfidf_random_forest_model/assets/

/analyzer_training/tfidf_random_forest_model/fingerprint.pb
/analyzer_training/tfidf_random_forest_model/keras_metadata.pb
# (If you DO want to keep only the final saved_model.pb, explicitly un-ignore it:)
!analyzer_training/tfidf_random_forest_model/saved_model.pb

# Any local IDE/editor files
.vscode/
*.swp
.idea/
*.iml

# Miscellaneous
*.log
.DS_Store
